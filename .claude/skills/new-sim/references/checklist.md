# 完成檢查表

全部打勾才算完成。把結果貼給用戶，未過的項目寫明原因。

## A. 物理正確
- [ ] 規格的每一條驗證條件都有一個同名測試，且通過
- [ ] 有解析解的量已與解析解比較（相對誤差 ≤ 1e-6）
- [ ] 應守恆的量（能量 / 動量 / 總動量）在應守恆的情況下漂移 ≤ 1e-6
- [ ] 方程、參數範圍、預設值與規格一致，沒有改動
- [ ] g 用共用 `gControl`，預設 9.81
- [ ] model.ts 沒有 import three.js / React；Scene.tsx 沒有計算物理，只畫 plan() 的結果

## A2. 畫面正確（plan.test.ts）
- [ ] 每支箭嘴的 vector 與模型量一致；加速度箭嘴等於模型的 a，不是速度差
- [ ] 合力 = 各力之和；法向反作用力 ⊥ 接觸面；摩擦沿接觸面且反相對運動趨勢；張力沿繩指離物體
- [ ] 同一 kind 全場景一個縮放係數；力與速度用不同係數但各自一致
- [ ] 標籤數值 = observe()；單位在允許清單內
- [ ] 隨機 50 組參數：無 NaN / Infinity，箭嘴長度有界
- [ ] 沒有自行換軸；方向類模擬與 handedness 基準截圖比對過
- [ ] 圖表 y 軸鍵都是 observe() 的鍵；軸單位正確

## B. 學生不用講解也懂得用
- [ ] 打開即在播放預設情境，畫面主體在中央，相機一眼看到規格「學生應該看見的現象」第 1 項
- [ ] 每個滑桿有中文標籤、符號、單位；範圍合理，拖到兩端都不會壞
- [ ] 規格的每一條迷思都有一個「試試看」情境，按下去畫面真的出現反差
- [ ] 存在一組參數令學生的錯誤預測與模擬結果明顯不同（反差原則）
- [ ] 暫停、逐格、重置、慢速可用
- [ ] 每支向量可獨立開關；速度與力的箭嘴線型明顯不同；顏色跟全站編碼
- [ ] 讀數三位有效數字、附指數式單位、符號斜體
- [ ] 桌面與 iPad 直向各截圖一張看過；觸控目標 ≥ 44 px；沒有只靠 hover 的互動
- [ ] console 沒有錯誤或警告

## C. 術語與內容
- [ ] `node tools/termlint.mjs` 對 guide 與 quiz 零錯誤
- [ ] 標題、標籤、guide 內每個物理名詞都查過術語表；查不到的已列入 `pendingTerms`
- [ ] guide.zh.md 五節齊全；常見混淆每條有「試試看」連結
- [ ] quiz.json 的 `sources` 是規格列出的題號；沒有自作題冒充真題
- [ ] 規格以外的內容已列入 `beyondSpec` 並在 guide 標明

## E. 可信性（對外可查證）
- [ ] 考題回歸：規格列出的每題 DSE / HKCEE 題目都有測試，把題目數值放入模型，輸出與評卷參考答案一致（`dse.test.ts`）；沒有評卷參考的題目列入 `pendingPapers`
- [ ] 真實數據對照（實驗類）：`content/labdata/<simId>/` 有學校實測數據時，誤差模式下的輸出分佈與之在實驗誤差內吻合（`labdata.test.ts`）
- [ ] 理想化假設在畫面固定位置列明（忽略空氣阻力、輕繩、無摩擦滑輪……），來自 `manifest.assumptions`
- [ ] `pendingTerms`、`beyondSpec`、`pendingPapers` 全部為空，否則只可內部預覽，不可發佈
- [ ] 第二實作比對報告「一致」（`reports/<simId>/second-impl.md`）
- [ ] 「驗證」分頁資料齊全：驗證條件與結果、考題回歸清單、核數日期、儀器審核結論、簽收人與日期、版本號（由 `reports/` 自動生成 `src/sims/<simId>/verification.json`）
- [ ] 老師簽收時已凍結基準：`baselines/<simId>/` 存有簽收版本的截圖與逐幀輸出，CI 的比對以此為準

## D. 收尾與獨立驗收
- [ ] `npx vitest run src/sims/<simId>` 全綠（model、plan、readout 三組測試）
- [ ] 物理核數 subagent：只讀規格與 observe() 輸出，獨立計算預期數值並比對，未讀過本模擬的代碼
- [ ] 學生試用 subagent：只用瀏覽器操作，未讀過代碼與規格，回報看不懂的地方；每項已處理或記錄
- [ ] 老師審核：截圖與「學生應該看見的現象」對照表已交老師；`beyondSpec`、`pendingTerms` 已列出
- [ ] catalogue.json 該項 `status: "done"`，已跑 `node tools/build-catalogue.mjs`
- [ ] 向用戶報告：做了甚麼、截圖、待老師定奪的項目

## F. 老師反饋：學生會唔明嘅畫面行為（每個模擬都要避免）
老師 2026-09-06 就 #023 提出，屬通用規則，Scene 設計時逐項對照：
- [ ] **軸／比例不隨時間改變。** 線圖軸範圍、軌道米／像素比例，都在重置時由參數預測的上界一次定好；播放期間不放大、不縮小。學生見到條線或物體突然跳，只會以為程式壞了或物理有錯。
- [ ] **物體離開畫面用鏡頭跟隨，不用縮比例。** 比例一縮，物體看似「瞬移」。鏡頭跟車＋會流動的背景（距離柱、樹、路面虛線）才令學生知道「車在動、走了多遠、向哪邊」。
- [ ] **線圖的 t 軸放在 y = 0 的軸線上**，不放在圖框底部；負值在軸線以下，與課本一致。刻度數字去尾零（10 而非 10.0）。
- [ ] 只有學生自己的操作（拖滑桿、改參數）才可以令畫面比例或軸改變；程式自動改的一律不准。
- [ ] 背景／地標必須在世界座標裏（跟物體同一比例移動），不可只是裝飾圖片。
