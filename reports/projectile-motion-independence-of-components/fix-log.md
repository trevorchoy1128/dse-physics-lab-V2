# 修正記錄：projectile-motion-independence-of-components

## 第 1 輪（v0.1.0 → v0.2.0，2026-09-06）

四份報告：physics-audit（物理通過；畫面 5.1、5.3 不通過）、second-impl（一致）、student-test（6 項）、apparatus-review（必改 3、建議 5、小事 6）。

### 已修正
| 來源 | 項目 | 改動 | 為何 |
|---|---|---|---|
| 核數員 5.3、儀器 必改 2 | 動能圖縱軸 100 J 標「1」 | Views.tsx、Scene.tsx：去尾零改為只去小數尾零（`fmt`） | 原正規表達式 `/\.?0+$/` 把整數的零也去掉 |
| 核數員 5.1 | 現象 1「一顆自由下落、一顆水平拋出」無截圖 | scenarios.ts：情境 1 改為 companion = "drop"（u = 10、θ = 0、h = 20）；guide 常見混淆 1 同步 | 水平初速 10 對 0 同樣是「水平初速不同」的反駁，且與規格現象 1 字面一致；截圖腳本會自動覆蓋 |
| 儀器 必改 1 | 發射器認不出 | Scene.tsx：發射管縮短至 1.2、管口在發射點；加半圓量角器（每 15° 刻度）、紅色角度指示線、θ 讀數；h = 0 時畫凹坑令管尾可見；「同時自由下落」的球改為夾持座 | 原管長 1.8、h = 0 時大半沉在地面下，只剩底板 |
| 儀器 必改 3 | 牆上投影與路徑同色分不開；牆寬固定 | plan.ts / Scene.tsx：牆與地面投影改灰色扁平圓點（wall / shadow），牆寬 1.6 × world；另加牆左緣高度影子列（ycol） | 投影是「影子」不是第二條路徑 |
| 儀器 建議 | a 與 W 箭嘴重疊、標籤疊成「aW」 | plan.ts：加速度 3.2、力 2.4 個世界單位；A_OFF 0.45 → 0.8 | m = 1 kg 時兩者數值相同，長度與偏移要刻意分開 |
| 儀器 建議 | 側視只顯示高度的影子列 | Views.tsx 側視 y 軸旁灰點列；3D 牆左緣同樣 | 對稱於地面影子（只顯示 x） |
| 儀器 建議 / 小事 | 比例尺與 x 標籤重疊；y 標籤遮發射器 | Scene.tsx 比例尺移到發射器正前方地面；y 標籤 x = −3.6；θ 標籤只在主發射器 | — |
| 學生 3 | 球停了時鐘仍在走 | types.ts `SimModel.done?`、runner.ts 全部落地後不再步進；model.ts `done` | 共用層小改（可選介面，不影響 #023）；主流程在場自行處理 |
| 學生 4 | 落地的球像浮在半空 | plan.ts 每顆球永遠有地面影子（gshadow） | 斜視缺少貼地線索 |
| 學生 5 | 讀不到第二顆球的水平距離 | charts.ts 讀數 x₂（有第二顆球時顯示；readouts.ts 加 `visible`） | — |

### 未改（附理由）
| 來源 | 項目 | 理由 |
|---|---|---|
| 核數員「待前端核對」 | early 截圖 3D 標籤比讀數早 0.03–0.05 s | 暫停狀態實機比對：標籤 x = 22.6 m、y = 0.00 m 與讀數逐字相同。差異只在播放中截圖的擷取時序（Html 覆蓋層與面板非同一幀合成），非渲染錯誤 |
| 學生 1 | 桌面尺寸（800 × 609）下方面板整片空白 | 以 800 × 609 重載：面板、試試看、指南全部正常，捲到底可見；無法重現。可能是它當時把 3D 畫布的空白天空當成面板，或分頁在背景時暫停繪製；留待第 2 輪學生試用者覆核 |
| 學生 2、6 | 「跳到 t =」數字輸入格逐字觸發、疑似打錯到 θ 欄位；「還原預設」一次出現 θ ≈ 5° | 共用層（SimShell 的數字輸入：每按一鍵即生效）。不在本模擬範圍；記入升級報告請老師決定是否改為 Enter 才生效 |
| 儀器 建議 | 頻閃照片模式（黑底只留影像） | 規格以外；列入 beyondSpec 候選，待老師定奪 |
| 儀器 建議 | 雙球落地器 | 「同時自由下落」現改為夾持座；是否要畫學校那款雙球落地器待老師定奪 |
| 儀器 小事 | 俯視視窗無發射器符號、無 z 刻度；地面格線無格距；球直徑逾 1 m | 球半徑是世界單位固定值（場景按射程縮放，球不隨之縮小，否則遠射程時看不見）；其餘為觀感，第 2 輪後視乎餘量處理 |
| 第二實作 待釐清 | k = 0.01 m⁻¹ 令 u = 50 時起始阻力 > g | 規格以外參數，不自行改；升級報告請老師定 k |

### 改動檔案
model.ts、plan.ts、Scene.tsx、Views.tsx、charts.ts、scenarios.ts、guide.zh.md、manifest.ts（version 0.2.0）、plan.test.ts、model.test.ts；共用層：src/shell/types.ts、runner.ts、readouts.ts。

### 重派
model → physics-auditor、second-implementer；plan / Scene → physics-auditor、apparatus-reviewer；scenarios / charts / guide → student-tester。第 2 輪四個全派。

## 第 2 輪（v0.2.0 → v0.2.1，2026-09-06）

四份報告：physics-audit-r2（物理通過；5.1a 情境 1 的 3D 看不到球；播放中 3D 標籤落後讀數）、second-impl-r2（一致）、student-test-r2（情境 1 看不到發射器；滾輪令視角錯亂；其餘共用層或無法重現）、apparatus-review-r2（第 1 輪三個必改已解決；新必改：h = 20 時發射塔伸出畫面）。

### 已修正
| 來源 | 項目 | 改動 | 為何 |
|---|---|---|---|
| 核數員 5.1a、儀器 必改、學生 3 | h = 20 時發射器與球起點在畫面外 | Scene.tsx 新增 `Framing`：按塔頂（含量角器）與路徑最高點自動設相機位置、軌道控制目標與距離上下限；只在場景範圍或 h 改變時重設 | 相機原本固定在 index.ts，場景高度隨 h、θ 變化 |
| 老師（本輪指示）、學生 1 | 視覺不要穿越地面；滾輪／拖動令視角錯亂 | `Framing` 設 maxPolarAngle 83°、minPolarAngle、minDistance / maxDistance | 視線不會轉到地面以下，縮放有上下限 |
| 核數員 待核對 | 播放中 3D 標籤落後讀數一至兩幀 | Scene.tsx `LiveLabel`：數值在 useFrame 內直接寫入 DOM | drei Html 子樹用另一個 React root 非同步渲染；共用 `Label` 元件同樣有此問題，另立任務（見下） |
| 老師 | x 標籤在地面以下 | plan.ts x 標籤移到球正前方地面上方 (y = 0.05, z = 1.4)；新增測試「所有標籤 y ≥ 0」 | — |
| 儀器 建議 | 高度影子列無標籤、與自由下落的球重疊 | Scene 加「高度（每 0.1 s）」標籤；plan 在 companion = drop 時不畫（該球本身就是高度列） | — |
| 儀器 建議 | 發射器太小、刻度看不到 | 量角器半徑 0.95 → 1.3，發射管 1.2 → 1.5，45° 刻度加粗 | — |
| 儀器 小事 | 俯視標籤蓋住影子點列；比例尺與早期 x 標籤相撞；側視 W / a 標籤相疊 | 俯視標籤移到地面前方（z = +1.8）；比例尺移到 x = 0.3 world；側視 a 箭嘴偏 12 px | — |

### 未改（附理由）
| 來源 | 項目 | 理由 |
|---|---|---|
| 學生 2 | 「跳到 t =」打字後按 Tab 令設定重置 | 共用層（SimShell）。本人以鍵盤重試未能重現；記入升級報告 |
| 學生 4 | g 與速度下拉選單無法切換 | 以 form_input 可切換；疑為試用者的點擊工具與原生 `<select>` 的互動限制；記錄待第 3 輪覆核 |
| 學生 5、6、7 | 分段按鈕要按兩次、開頁已是結束畫面、iPad 一次 3D 空白 | 無法重現；開頁即播放在本人多次重載均正常；記錄 |
| 核數員 | 共用 `Label` 元件的一至兩幀落後 | 共用層問題，影響所有用 Label 的模擬；另立任務改為 useFrame 寫入 |
| 儀器 待問 | 雙球落地器、頻閃照片模式、球尺寸、「質點」用語 | 待老師定奪，列入升級報告 |

### 改動檔案
plan.ts、Scene.tsx、Views.tsx、manifest.ts（0.2.1）、plan.test.ts。model.ts 未改。

### 重派
plan / Scene → physics-auditor、apparatus-reviewer（第 3 輪）。

## 第 3 輪後（v0.2.2 → v0.2.3，2026-09-06，未經驗收員覆核）

- 儀器 必改（取景裁掉 y 標籤與比例尺）：`Framing` 包圍盒加入標籤與比例尺、8% 邊距、相機放低；量角器弧與刻度同一範圍；x、y 標籤置中。
- 核數員 C3（藍球起始被塔頂遮住）：夾持座塔身後退、夾臂伸到發射點上方。
- 老師：「大炮的模型很差」→ 發射器重做（v0.2.3）：底板、兩塊支架、橫軸、口徑比球大的發射管（炮口環、尾蓋）、軸側半透明量角器與紅色指針。**設計取捨：樞軸在發射點，管向前伸出**，球由管口離開，h = 0 時器材仍全在地面上（之前管口為發射點，h = 0 時大半沉在凹坑內）。已列入升級報告第 9 項請老師確認。
- 核數員 C2（3D 標籤恆定落後讀數一格）：外殼與 three.js 各一條 RAF 迴圈的次序所致，實機 60 fps 為 1/60 s；不在本模擬範圍，升級報告第 11 項。

## 第 4 輪（老師 2026-09-06 決定後，v0.3.0 → v0.3.1）

四份報告：physics-audit-r4（物理通過；3.14 有阻力時 ½mvₓ² 參考線錯、6.1 小質量有阻力時時間窗短於落地）、second-impl-r4（一致，改用閉式解）、student-test-r4（五個任務完成；設定無故重設、球「消失」、下拉選單）、apparatus-review-r4（取景已解決、發射器認得出；必改：指南第 1 步仍寫兩顆球）。

### 已修正
| 來源 | 項目 | 改動 |
|---|---|---|
| 核數員 3.14 | 有阻力時 ½mvₓ² 參考線用無阻力值 | Views.tsx：有阻力時不畫參考線（vₓ 隨時間減小，最低點不再是 ½m(u cosθ)²） |
| 核數員 6.1 | 有阻力、小質量時時間窗短於落地 | plan.ts 新增 `flightTimeDrag`（線性阻力閉式解 y(t) 的正根，二分法），duration = 落地時刻 + 0.3 s；新增測試「duration ≥ 落地時刻」 |
| 儀器 必改 | 指南第 1 步與預設不符 | guide.zh.md 第 1 步改為一顆球，描述地面影子等距、牆上高度影子疏密 |
| 學生 2 | 取消速度箭嘴後「球消失」 | 實為 t = 0 時球在發射管內（設計取捨第 9 項）；發射管改半透明（opacity 0.62）、管身淺鋼灰與底板分色、量角器半徑 1.0 |
| 學生 1 | 操作中設定無故重設、畫面閃白 | 本人未改 src；核數員同時在 reports/ 寫檔令 Vite 整頁重載（核數員第 3 輪已觀察到）。vite.config.ts 的 server.watch 排除 reports/ 與 baselines/ |

### 未改（附理由）
| 來源 | 項目 | 理由 |
|---|---|---|
| 學生 3 | 有阻力 + 加倍時藍球「在半空」而 y₂ = 0 | 線性阻力下 y ≡ yB（核數員與第二實作者皆核實差 0），同時落地；藍球在 z = −3 的遠處，斜視像離地；有地面影子。記錄，待第 5 輪儀器審核看截圖 |
| 學生 4 | 兩個下拉選單改不到 | 試用者的點擊工具與原生 `<select>` 的限制（已提示用 form_input）；本人以 form_input 可切換 |
| 核數員 §7 | H 在阻力下仍為無阻力公式（未顯示） | 不在面板顯示，供核數；註明「無阻力時的最高點」 |
| 第二實作 | m = 0.1 kg 時終端速率 3.3 m s⁻¹，球很快失速 | 老師定「阻力要明顯」；屬正常物理，指南第 8 步已註明質量愈大影響愈小 |

### 改動檔案
plan.ts、Views.tsx、Scene.tsx、guide.zh.md、manifest.ts（0.3.1）、plan.test.ts；vite.config.ts（開發伺服器監視範圍）。model.ts 未改。

### 重派
plan / Scene / guide → physics-auditor、apparatus-reviewer（第 5 輪，老師第 1 項「派人再處理」的延續）。
