# 升級：motion-graphs-synced-with-real-motion（第 7 輪後，v0.5.1）

閘 2 已跑七輪（規則三輪 + 老師批准三輪 + 2026-09-06 老師批准第 7 輪，並授權按需要跑至第 9 輪）。第 5、6 輪物理核數員**零失敗**，第二實作者第 1、2、4、5 輪**全部一致**，第 6 輪學生試用者**三個任務全部完成**。

## 各輪結果
| 輪 | 物理核數員 | 第二實作者 | 學生試用者 | 修正 |
|---|---|---|---|---|
| 1 | 數值通過；F1–F3 | 一致 | 7 項卡住（iPad 卡死、模式下拉、時間窗重置、圓點難拖、無跳時） | 0.2.0：Canvas2D 定位、分段按鈕、時間拉桿、逐格 |
| 2 | F1、F3 已修；F2 未修；F4 | 一致 | 6 項（拉桿只用一次、y 軸跳動、逐格 0.001 s） | runner.seek、軸固定、useLayoutEffect |
| 3 | F2、F4 已修；F5、F6 | — | 6 項（軸因滑桿跳動、無「跳到 t」、拖點無數值） | 0.3.0：歸零、軸只放大、跳到 t、拖點數值 |
| 4 | F5 已修；F6 殘餘、F7 箭嘴消失、F8 比例 | 一致 | 三任務完成；6 項（重置不還原、播放列在下方） | 0.4.0：末節點 NaN、凍結 t = T、播放列上移、還原預設 |
| 5 | **通過** | 一致 | 三任務完成；6 項（Tab 誤觸、拖點、殘餘顯示） | 0.4.1：1 s 推進上限、就近拖動、meta 同源 |
| 6 | **通過** | — | 三任務完成；7 項（引導、路程解釋、欄位捲動） | 第 6 輪後：上色、三步提示、讀數解釋、右欄固定 |
| 7（v0.5.0：鏡頭跟車、t 軸在 y = 0、軸固定、T 2–60 s） | 數值全過（最大相對誤差 3.8e-10，T = 35.9 s 亦然）；F9 畫圖模式 s 軸未計控制點以外的保持段；F10 凍結幀 a 取錯段 | 一致（10 運行 × 8 鍵 × 60001 幀 ≤ 1e-6） | 電腦：任務 1、2 完成，任務 3 三次「自動跳回模式一」；iPad：三任務完成；地面刻度範圍會變 | 0.5.1：F9 逐段精確極值 + 保持段；鏡頭偏離時顯示「鏡頭跟着小車移動（比例不變）」；距離柱數字 12 px；Guide 閉包錯誤 |

每輪「播放極慢」「無端端重置」「舊模組錯誤」三項為測試環境所致（分頁在背景時瀏覽器暫停動畫；派發者改動 src 觸發熱更新；Vite 熱更新後舊模組殘留），已寫入 loop.md 第 7 節，並把每幀推進上限提高至 1 s 令背景視窗仍保持真實時間。

## 第 7 輪裁決
- **F10 判為核數員誤報**（loop.md §4：核數員不通過、第二實作一致 → 用解析值裁決）。核數員的例子 vt = [1, −1, 1, …]、T = 2：t ∈ [1, 2) 一段 v 由 −1 升到 +1，斜率 = (1 − (−1)) / 1 = **+2**，凍結幀 a = +2 正是「小車剛走完那段」的斜率；核數員把該段斜率誤記為 −2。實測（reports/…/audit/f10-repro.mts）：t = 1.5 s a = 2、t = 1.999 s a = 2、t = 2 s（凍結）a = 2。主實作 observe 用 accel(min(t, T − 1e-9)) 一律取左段，與 T = 10 末節點的規則一致。
- **學生試用者「電腦版按播放後自動跳回模式一」判為測試環境所致**：同一 repo 有另一個 session 在 06:24–06:26 改動 src/sims/projectile-…/（Scene、Views、manifest、plan、plan.test），該目錄經 registry.ts 進入 Vite 模組圖，熱更新令頁面重載、參數回到預設（模式一、a = 1 自動播放），與試用者描述完全吻合。派發者本人於 06:35 在同一伺服器以電腦尺寸重做任務 3：切換模式、拖點、暫停、再播放，模式保持、小車按圖行走，無異常。「模式按鈕要按兩次」「t 突然跳 2 s」同源（重載 + 分頁在背景時的追趕）。console 的 4 個 `Cannot read properties of null (reading '2')` 只出現於該瀏覽器分頁的硬重載，Playwright 乾淨載入 0 錯誤；順手修正 Guide.tsx 內一個確實存在的閉包錯誤（試試看連結點擊時 m 已為 null）。**第 8 輪學生試用改用 vite build + preview（無熱更新）的靜態伺服器**，杜絕此類干擾。
- 學生試用者其餘意見已修：地面刻度範圍隨鏡頭移動令人以為縮放 → 鏡頭偏離起點時顯示「鏡頭跟着小車移動（比例不變）」；距離柱數字加大至 12 px。「電腦版字太小」是瀏覽器窗格被縮到 800 × 317 所致，1440 × 900 下正常。

## 第 6 輪後的改動（已由第 7 輪覆核）
- 老師要求「畫面 colorful 一點」：天空 / 草地 / 路面配色、琥珀色小車、起點旗、三張圖各自色系與色條、單元主色（首頁格子、標題列、按鈕、試試看）。向量顏色編碼不變。
- 首次進入三步提示（可關閉，記在瀏覽器）；讀數「位移」「路程」加一句解釋；v–t 控制點下方標明秒數；右欄固定不隨頁面捲動。
- 導出數據加入每幀 meta（核數員第 6 輪建議）。
- **0.4.2（老師 2026-09-06 要求）**：軌道比例不再隨位移放大（舊做法令小車看似「瞬移」），改為固定米／像素、鏡頭跟車（小車離開畫面中央 40% 區域時鏡頭移動，重播回到起點），背景加距離柱（附米數）、樹、路面虛線、視差雲，讓學生看到「車在動、景在退」。畫面橫跨的米數由參數預測的最大速率決定（3 × vmax 取好看數，介乎 10–100 m），運行中不變。三張線圖的 t 軸刻度與數字移到 y = 0 軸線上，「t / s」放軸線另一側。刻度數字去尾零（10 而非 10.0）。iPad 直向軌道最少 170 px。線圖軸範圍不再「見到多少放大多少」（學生會不明白條線為何突然跳），改為重置時由參數預測的上界一次定好，播放期間不變；只有學生自己拖即時加速度滑桿才可能放大。

## 我的判斷
物理與畫面向量已由核數員連續兩輪零失敗確認；餘下的全是引導與觀感，已按第 6 輪報告修正。上色、引導與 0.4.2 的鏡頭跟車屬畫面層改動，按規則應重派核數員（截圖對照）與學生試用者各一次。鏡頭跟車尤其要由學生試用者確認「知道車向哪邊走、走了多遠」。

## 請老師決定
1. 上色與引導的改動：接受並簽收，或批准第 7 輪只派核數員與學生試用者覆核。
2. 規格無參數表：初速 ±5 m s⁻¹、加速度 ±10 m s⁻²、時間窗 5 / 10 / 20 s。
3. 「由圖生成運動」用每秒一個控制點（步進 0.5 m s⁻¹），按圖框內任何位置即拖動該秒。
4. 時間窗末端凍結後 a 保持最後一段的值；時間窗長於控制點範圍時 v 保持末值。
5. 術語：「s–t 圖」「v–t 圖」「線下面積」「切線」沿用規格用語。
6. 考題回歸：八題待評卷參考，歷屆試卷 PDF 為掃描圖像無法抽字；未有前狀態只能是「內部預覽」。
